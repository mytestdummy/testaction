name: 'Testaction'
author: 'Sitdisch'
description: 'Test your action'
inputs:
  who-to-greet: # id of input
    description: 'test'
    required: true
    default: 'World'
runs:
  using: "composite"
  steps:
    - run: 
        # COMMIT_MESSAGE='$(git log --format=%B -n 1 HEAD~4)'
        echo "$(git log --format=%B -n 1 HEAD)"
        echo hallo
        # touch ${{ inputs.who-to-greet }}
        # git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
        # git config --local user.name "github-actions[bot]"
        # git add .
        # git commit -am $COMMIT_MESSAGE
        # git push
      shell: bash

# RESIDUUM
    # - run: ${{ github.action_path }}/goodbye.sh
    #   shell: bash
    # - run: |
    #     git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
    #     git config --local user.name "github-actions[bot]"
    #     git add .
    #     git commit -am "Repository History Cleaned | Monthly"
    #   shell: bash
    # - uses: ad-m/github-push-action@master
